<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <Preference
        android:key="cache_management"
        android:title="Quản lý cache"
        android:summary="Xem và xóa snapshot đã lưu" />

    <SwitchPreferenceCompat
        android:key="storage_access"
        android:title="Cấp quyền Storage"
        android:summary="Chưa cấp quyền · ứng dụng sẽ dùng SAF"
        android:defaultValue="false" />

    <ListPreference
        android:key="markdown_model"
        android:title="Mô hình Markdown"
        android:summary="Chọn mô hình hiển thị cây"
        android:entries="@array/markdown_models"
        android:entryValues="@array/markdown_model_values"
        android:defaultValue="D" />

    <SwitchPreferenceCompat
        android:key="skip_hidden_folders"
        android:title="Bỏ qua thư mục ẩn"
        android:summary="Không quét các thư mục bắt đầu bằng dấu chấm"
        android:defaultValue="true" />

    <SwitchPreferenceCompat
        android:key="skip_hidden_files"
        android:title="Bỏ qua file ẩn"
        android:summary="Không quét các file bắt đầu bằng dấu chấm"
        android:defaultValue="true" />

    <SwitchPreferenceCompat
        android:key="skip_system_junk"
        android:title="Bỏ qua rác hệ thống"
        android:summary="Không quét .thumbnails, .cache, cache, .temp, .trash"
        android:defaultValue="true" />

    <EditTextPreference
        android:key="max_depth"
        android:title="Độ sâu tối đa"
        android:summary="Để trống = không giới hạn"
        android:inputType="number"
        android:defaultValue="" />

    <ListPreference
        android:key="sort_order"
        android:title="Sắp xếp mặc định"
        android:summary="Thứ tự sắp xếp các mục"
        android:entries="@array/sort_orders"
        android:entryValues="@array/sort_order_values"
        android:defaultValue="name" />

    <SwitchPreferenceCompat
        android:key="show_metadata_in_md"
        android:title="Hiện metadata trong Markdown"
        android:summary="Hiện kích thước, ngày sửa trong file MD"
        android:defaultValue="false" />

    <SwitchPreferenceCompat
        android:key="notification_on_scan_complete"
        android:title="Thông báo khi quét xong"
        android:summary="Hiện thông báo khi quét hoàn tất"
        android:defaultValue="true" />

</PreferenceScreen>
